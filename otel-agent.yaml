receivers:
  jaeger:
    protocols:
      thrift_compact:

exporters:
  otlp:
    endpoint: 127.0.0.1:4317
    tls:
      insecure: true

processors:

service:
  pipelines:
    traces:
      receivers: [jaeger]
      processors: []
      exporters: [otlp]
